# Architecture Diagrams

## System Overview

```
┌────────────────────────────────────────────────────────────────┐
│                        CLOUD ☁️                                 │
│  Firebase Realtime Database + Firestore                        │
│  (Device commands, sensor history, status)                     │
└─────────────────────────────┬──────────────────────────────────┘
                              │
                      [Firebase Admin SDK]
                              │
┌─────────────────────────────▼──────────────────────────────────┐
│                     RASPBERRY PI                               │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │  main.py (ENTRY POINT)                                  │ │
│  │  └─ asyncio.run(main())                                 │ │
│  └─────────────┬──────────────────────────────────────────┘ │
│                │                                             │
│  ┌─────────────▼──────────────────────────────────────────┐ │
│  │  src/core/server.py (ORCHESTRATOR)                    │ │
│  │  └─ RaspServer class                                   │ │
│  │     ├─ Initializes services                           │ │
│  │     ├─ Runs sensor loop                               │ │
│  │     └─ Routes commands                                │ │
│  └─────────────┬──────────────────────────────────────────┘ │
│                │                                             │
│  ┌─────────────▼──────────────────────────────────────────┐ │
│  │  src/services/ (BUSINESS LOGIC)                        │ │
│  │  ├─ FirebaseService (cloud sync)                      │ │
│  │  ├─ SensorService (validation)                        │ │
│  │  └─ AutomationService (scheduling)                    │ │
│  └─────────────┬──────────────────────────────────────────┘ │
│                │                                             │
│  ┌─────────────▼──────────────────────────────────────────┐ │
│  │  src/controllers/ (HARDWARE CONTROL)                   │ │
│  │  ├─ SensorController (read sensors)                   │ │
│  │  ├─ IrrigationController (pump)                       │ │
│  │  ├─ LightingController (LEDs)                         │ │
│  │  └─ HarvestController (motors)                        │ │
│  └─────────────┬──────────────────────────────────────────┘ │
│                │                                             │
│  ┌─────────────▼──────────────────────────────────────────┐ │
│  │  GPIO/PWM/I2C (PHYSICAL HARDWARE)                      │ │
│  │  ├─ DHT22 sensor (I2C)                                │ │
│  │  ├─ Pump relay (GPIO + PWM)                           │ │
│  │  ├─ LED PWM (GPIO)                                    │ │
│  │  └─ Motors (GPIO + PWM)                               │ │
│  └─────────────┬──────────────────────────────────────────┘ │
│                │                                             │
│                ▼                                             │
│        [Physical Hardware 🌱]                               │
│        ├─ Sensors (temperature, humidity, water)          │
│        ├─ Pump (irrigation)                               │
│        ├─ LEDs (lighting)                                 │
│        └─ Motors (harvest belts)                          │
└────────────────────────────────────────────────────────────────┘
```

---

## Request/Response Cycle

### Cloud Command → Hardware Activation

```
USER (Mobile App / Web Dashboard)
   │
   ▼
[Cloud] Firebase Console
   │
   │ Write to /devices/{device_id}/commands/
   │
   ▼
┌─────────────────────────────────────────────────┐
│ RaspServer (listening to Firebase)              │
│                                                 │
│ FirebaseService._listen_for_commands()          │
│   └─ on_command_update() triggered              │
│      └─ _route_command()                        │
│         └─ Find registered handler              │
└─────────────────┬───────────────────────────────┘
                  │
        Handler: _handle_irrigation_start()
                  │
                  ▼
┌─────────────────────────────────────────────────┐
│ RaspServer._handle_irrigation_start()            │
│   └─ asyncio.create_task(...)                   │
│      └─ IrrigationController.start()            │
└─────────────────┬───────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────┐
│ IrrigationController                            │
│   └─ GPIO.setup(PUMP_PWM_PIN, ...)              │
│   └─ PWM.start(80)  # 80% speed                 │
└─────────────────┬───────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────┐
│ Hardware (GPIO/PWM)                             │
│   └─ Pump relay activated                       │
└─────────────────┬───────────────────────────────┘
                  │
                  ▼
            💧 PUMP RUNS 💧
                  │
                  ▼
           (Status published back to Firebase)
```

---

## Data Collection & Publishing

### Sensor Reading Loop

```
Every 5 seconds (SENSOR_READING_INTERVAL):

┌──────────────────────────────────────────────┐
│ RaspServer._sensor_reading_loop()            │
└────────────────┬─────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────────┐
│ SensorService.read_all()                     │  ← Business Logic
│   └─ Calls SensorController.read_all()      │
└────────────────┬─────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────────┐
│ SensorController.read_all()                 │  ← Hardware Reading
│   ├─ DHT22.read() → (temp, humidity)        │
│   ├─ GPIO.input(WATER_LEVEL_PIN)            │
│   └─ Return raw values                      │
└────────────────┬─────────────────────────────┘
                 │
                 ▼
         Raw Sensor Data
         ├─ Temperature: 72.5°F
         ├─ Humidity: 65%
         ├─ Soil: 70%
         └─ Water: True
                 │
                 ▼
┌──────────────────────────────────────────────┐
│ Return SensorReading (typed model)          │  ← Data Structure
└────────────────┬─────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────────┐
│ RaspServer continues...                     │
│   └─ check_thresholds(reading)              │
└────────────────┬─────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────────┐
│ SensorService.check_thresholds()            │  ← Validation
│   ├─ Compare temp to TEMP_MIN/MAX           │
│   ├─ Compare humidity to HUMIDITY_MIN/MAX   │
│   ├─ Compare soil to SOIL_MOISTURE_MIN/MAX  │
│   └─ Check water_level critical             │
└────────────────┬─────────────────────────────┘
                 │
                 ▼
          Alerts Generated
          (if thresholds exceeded)
                 │
                 ▼
┌──────────────────────────────────────────────┐
│ FirebaseService.publish_sensor_data()       │  ← Publish
│   ├─ Write to Realtime DB: sensors/latest   │
│   └─ Write to Firestore: sensor_history     │
└────────────────┬─────────────────────────────┘
                 │
                 ▼
         📊 Data in Cloud ☁️
         ├─ Real-time: app sees latest
         └─ Historical: analytics
```

---

## Service Layer Details

```
┌─────────────────────────────────────────────────────────────┐
│                    SERVICES LAYER                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ FirebaseService                                     │  │
│  ├─────────────────────────────────────────────────────┤  │
│  │ Responsibilities:                                   │  │
│  │ • Connect to Firebase Admin SDK                     │  │
│  │ • Publish sensor data to Realtime DB & Firestore  │  │
│  │ • Listen for commands from cloud                   │  │
│  │ • Handle authentication/authorization              │  │
│  │ • Manage device status (online/offline)            │  │
│  │                                                     │  │
│  │ Key Methods:                                        │  │
│  │ • connect() - Initialize Firebase                 │  │
│  │ • publish_sensor_data(reading) - Send data        │  │
│  │ • register_command_handler() - Listen for cmd      │  │
│  │ • _listen_for_commands() - Stream from DB          │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ SensorService                                       │  │
│  ├─────────────────────────────────────────────────────┤  │
│  │ Responsibilities:                                   │  │
│  │ • Read sensors via SensorController                │  │
│  │ • Validate readings against thresholds             │  │
│  │ • Generate alert objects                           │  │
│  │ • Provide structured SensorReading data            │  │
│  │                                                     │  │
│  │ Key Methods:                                        │  │
│  │ • read_all() → SensorReading                       │  │
│  │ • check_thresholds(reading) → list[ThresholdAlert]│  │
│  └─────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ AutomationService                                   │  │
│  ├─────────────────────────────────────────────────────┤  │
│  │ Responsibilities:                                   │  │
│  │ • Run automation loop (scheduling)                 │  │
│  │ • Check time-based rules (IRRIGATION_SCHEDULE)    │  │
│  │ • Control devices on schedule                      │  │
│  │ • No state persistence (read config each time)    │  │
│  │                                                     │  │
│  │ Key Methods:                                        │  │
│  │ • run_automation_loop() - Main loop               │  │
│  │ • _check_irrigation_schedule(time)                │  │
│  │ • _check_lighting_schedule(time)                  │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## Data Models

```
┌─────────────────────────────────────────────────────────────┐
│                      DATA MODELS                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  SensorReading (src/models/sensor_data.py)                │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ @dataclass                                          │  │
│  │ ├─ timestamp: str (ISO format)                      │  │
│  │ ├─ temperature: float (°F)                          │  │
│  │ ├─ humidity: float (%)                              │  │
│  │ ├─ soil_moisture: float (%)                         │  │
│  │ ├─ water_level: bool                                │  │
│  │ └─ simulation: bool (debug mode)                    │  │
│  │                                                     │  │
│  │ Methods:                                            │  │
│  │ └─ to_dict() / to_json()                            │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                             │
│  ThresholdAlert (src/models/sensor_data.py)               │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ @dataclass                                          │  │
│  │ ├─ severity: str ("warning" or "critical")         │  │
│  │ ├─ sensor_type: str ("temperature", etc)           │  │
│  │ ├─ current_value: float (actual reading)            │  │
│  │ ├─ threshold: float (exceeded limit)                │  │
│  │ └─ timestamp: str (when detected)                   │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                             │
│  Command (src/models/command.py)                          │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ @dataclass                                          │  │
│  │ ├─ category: str ("irrigation", "lighting", etc)   │  │
│  │ ├─ action: str ("start", "stop", "on", "off")     │  │
│  │ ├─ params: dict (command arguments)                 │  │
│  │ └─ timestamp: str (when sent)                       │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                             │
│  DeviceStatus (src/models/command.py)                     │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ @dataclass                                          │  │
│  │ ├─ device_id: str                                   │  │
│  │ ├─ status: str ("online" or "offline")             │  │
│  │ ├─ last_seen: str (ISO timestamp)                   │  │
│  │ └─ current_operation: str (optional)                │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## Class Relationships

```
                        RaspServer
                    (src/core/server.py)
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
        ▼                  ▼                  ▼
   Controllers          Services           Models
        │                  │                  │
        ├─ Sensors        ├─ Firebase       ├─ SensorReading
        ├─ Irrigation     ├─ Sensor         ├─ ThresholdAlert
        ├─ Lighting       └─ Automation     ├─ Command
        └─ Harvest                          └─ DeviceStatus
        
        
          RaspServer Initialization
          
    server = RaspServer()
        │
        ├─ self.firebase = FirebaseService()
        ├─ self.sensors = SensorService()
        │   └─ uses SensorController internally
        ├─ self.automation = AutomationService(
        │                      irrigation, lighting)
        │   └─ uses IrrigationController
        │   └─ uses LightingController
        ├─ self.irrigation = IrrigationController()
        ├─ self.lighting = LightingController()
        └─ self.harvest = HarvestController()
        
            ↓
            
    server.firebase.register_command_handler(
        "irrigation", "start", 
        server._handle_irrigation_start
    )
```

---

## Execution Flow: Application Startup

```
1. python main.py
        │
        ▼
2. asyncio.run(main())
        │
        ▼
3. server = RaspServer()
        │
        ├─ Create controllers
        ├─ Create services
        ├─ Register command handlers
        └─ Initialize logger
        │
        ▼
4. signal.signal(...) - Setup shutdown handlers
        │
        ▼
5. await server.start()
        │
        ├─ firebase.connect()
        │  └─ Initialize Firebase Admin SDK
        │  └─ Listen for commands
        │
        ├─ Start concurrent tasks:
        │  ├─ _sensor_reading_loop()
        │  │  Every 5 seconds:
        │  │  ├─ Read sensors
        │  │  ├─ Check thresholds
        │  │  ├─ Publish to Firebase
        │  │  └─ Handle alerts
        │  │
        │  └─ automation.run_automation_loop()
        │     Every minute:
        │     ├─ Check irrigation schedule
        │     └─ Check lighting schedule
        │
        └─ Keep running until stop() called
        │
        ▼
6. Graceful shutdown (Ctrl+C)
        │
        ├─ Stop all hardware
        ├─ Disconnect Firebase
        └─ Cleanup GPIO
```

---

## Error Handling Pattern

```
try:
    await some_operation()
    
except HardwareError:
    logger.error("Hardware failed")
    # Recover or alert
    
except FirebaseError:
    logger.error("Cloud sync failed")
    # Retry or fallback
    
except Exception as e:
    logger.error(f"Unexpected error: {e}")
    # Log and continue
    
finally:
    # Cleanup if needed
```

---

This modular architecture makes your code:
- **Testable** - Each layer independent
- **Maintainable** - Clear responsibilities
- **Scalable** - Add features easily
- **Professional** - Industry standard patterns
